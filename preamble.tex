\documentclass{article}

\usepackage[dvipsnames]{xcolor}
\definecolor{AHKeywordColour}    {cmyk}{0.86,0.91,0,0.04}
\definecolor{AHFunctionColour}   {cmyk}{0.64,0,0.95,0.40}
\definecolor{AHConstructorColour}{cmyk}{0,0.83,1,0.70}
\definecolor{AHNumberColour}     {HTML}{A020F0}
\definecolor{AHStringColour}     {HTML}{B22222}

\PassOptionsToPackage{full}{textcomp}

% I prefer this font to the normal one.

% This package is used later for the agda file.
% We need to import it here to set the dvipsnames option.
\usepackage[dvipsnames]{xcolor}

% Tikz
\usepackage{tikz}


% For some of the operators I define myself I use this package.
\usepackage{pict2e}

% This package lets us import code snippets into the document.
\usepackage{catchfilebetweentags}

% Packages with maths symbols.
\usepackage{amsmath}
\usepackage{mathtools}
\usepackage{amssymb}
\usepackage{turnstile}
\usepackage{stmaryrd}
\usepackage{latexsym}

% Theorem environments
\newtheorem{definition}{Definition}

% Maths fonts
\usepackage{mathrsfs}
\usepackage{bbm}

% Needed to typeset Agda's stuff.
\usepackage[english]{babel}

% Handy package for on-the-fly resizing.
\usepackage{relsize}

% Does links and stuff.
% \usepackage{hyperref}
\usepackage{cleveref}

% Citations
% \usepackage{natbib}

% Multiple figures
\usepackage{subcaption}
\usepackage{dblfloatfix}

\usepackage[backgroundcolor=Lavender,textwidth=1.15cm]{todonotes}
\setlength{\marginparwidth}{1.22cm}
\presetkeys{todonotes}{inline}{}
\newcommand{\mtodo}[1]{\todo[noinline,noline,size=tiny]{\tiny #1}}

% \PrerenderUnicode{í}

\input{agda}
\input{haskell}
